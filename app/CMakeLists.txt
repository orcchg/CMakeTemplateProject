SET( TARGET_NAME AppTemplate )
SET( SOURCE_DIR ${CMAKE_CURRENT_LIST_DIR} )

SET( SOURCES
    ${SOURCE_DIR}/main.cpp
)

ADD_EXECUTABLE( ${TARGET_NAME} ${SOURCES} )
TARGET_LINK_LIBRARIES( ${TARGET_NAME} LibraryTemplate ${OPENCL_LIBRARIES} ${OpenCV_LIBRARIES} )

